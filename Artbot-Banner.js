// Artbot Banner JavaScript
document.addEventListener('DOMContentLoaded', function() {
    console.log('Artbot Banner loaded successfully');
    
    // Initialize banner functionality
    initBanner();
});

function initBanner() {
    const bannerContent = document.querySelector('.banner-content');
    
    if (bannerContent) {
        // Add any banner-specific initialization here
        console.log('Banner content found and initialized');
    }
} 